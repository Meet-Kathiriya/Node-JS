<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            padding: 20px;
            font-family: Arial, sans-serif;
            background: #f7f7f7;
            color: #222;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background: #fff;
            padding: 16px;
            border: 1px solid #ddd;
            border-radius: 6px;
        }

        h1 {
            margin: 0 0 12px;
            font-size: 22px;
        }

        .subtitle {
            color: #666;
            margin-bottom: 12px;
        }

        form {
            display: flex;
            gap: 8px;
        }

        input[type="text"] {
            flex: 1;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            padding: 8px 12px;
            border: 1px solid #0d6efd;
            background: #0d6efd;
            color: #fff;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background: #0b5ed7;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Edit Item</h1>
        <div class="subtitle">Update your to-do item below.</div>

        <form action="/updateData" method="post">
            <input type="text" value="<%=singleData.id%>" hidden placeholder="Enter Your Name" name="id">
            <input type="text" value="<%=singleData.name%>" placeholder="Update your item" name="name">
            <button type="submit">Save</button>
        </form>
    </div>
</body>

</html>